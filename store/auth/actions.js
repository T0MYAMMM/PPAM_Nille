export const apiLoginRequest = ({username, password}) => {
    // Disini Anda akan membuat request ke firebase untuk login
}

export const apiSignupRequest = ({email, password}) => {
    // Disini Anda akan membuat request ke firebase untuk signup
}